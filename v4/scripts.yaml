# ---------------------------------------------------------
  light_on_dev60:
    sequence:
      - condition: state 
        entity_id: light.dev60
        state: 'off'
      - service: light.turn_on
        entity_id: light.dev60
        
  light_off_dev60:
    sequence:
      - condition: state 
        entity_id: light.dev60
        state: 'on'
      - service: light.turn_off
        entity_id: light.dev60
# ---------------------------------------------------------